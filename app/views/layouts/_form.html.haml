.gray
  = form_for @message, :remote => true do |f|
    -if @message.errors.any?
      #error_explanation
        %h2= "#{pluralize(@message.errors.count, "error")} prohibited this message from being saved:"
        %ul
          - @message.errors.full_messages.each do |msg|
            %li= msg


    -#.field
      -#-#= f.label :content
      -#= f.text_area :content
    -#.field
      -#= f.label "昵称"
      -#= f.text_field :user
    -#.actions
      -#= f.submit '发送'




    .grid_7
      -#= f.label :content
      = f.text_area :content, :class => "send_form"
    .grid_2
      = f.label "昵称", :class => "nickname_lable"
      = f.text_field :user, :class => "nickname_text"
      = f.submit '发送', :class => "clean-gray send_button"
